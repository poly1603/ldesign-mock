{
  "name": "@ldesign/mock-workspace",
  "version": "1.0.0",
  "private": true,
  "description": "Mock 工具 workspace",
  "scripts": {
    "build": "pnpm -r --filter=\"./packages/**\" run build",
    "dev": "pnpm -r --parallel run dev",
    "clean": "pnpm -r run clean",
    "test": "pnpm -r run test"
  },
  "engines": {
    "node": ">=16.0.0",
    "pnpm": ">=8.0.0"
  }
}

